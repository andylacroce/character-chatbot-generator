/**
 * Re-exports the canonical color/brand tokens from character-chatbot-shared —
 * see that package's src/theme.ts for the values (mirrors the web app's
 * globals.css/darkmode.css) and why they live there instead of being
 * hand-copied per client.
 */
export { lightColors, darkColors, BRAND, type ThemeColors } from "character-chatbot-shared";
