{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // TypeScript 6 no longer auto-includes every @types package; the tests need Jest's globals.
    "types": ["jest"]
  }
}
